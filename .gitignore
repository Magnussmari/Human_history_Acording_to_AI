# Secrets and auth
.env
.env.local
docker/.env
*.lock

# Claude Code — commit shared config, ignore local state
.claude/state/
.claude/local/
.claude/**/*.tmp

# Temp files
*.tmp
state/lock/

# Docker build cache
docker/.docker/
